# Confidential — assessments and answer keys NEVER go to GitHub.
# House standard: https://tertiarycourses.github.io/wsqcourseware/
# They are distributed via Google Drive only (anyone-with-link viewer).
assessment/
reference/

# Local scratch
.DS_Store
__pycache__/
*.pyc

# Distribution archives (rebuildable — never pushed to GitHub)
*.zip

# Playwright MCP scratch
.playwright-mcp/

# Stray Playwright captures at repo root (course assets live in courseware/assets/)
/sipoc-*.png
/diag.png

# LibreOffice duplicate-render artefacts (never shipped)
*[0-9].docx.bak
* 2.docx
* 2.pdf

# Local environment — internal Drive links, never pushed
.env
.env.*
!.env.example
!.env.sample
